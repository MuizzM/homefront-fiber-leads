interface:
  display_name: "Homefront Scanner Safety"
  short_description: "Guard provider scans, budgets, and tenants"
  default_prompt: "Use $homefront-scanner-safety to inspect this scanner change before implementation."
policy:
  allow_implicit_invocation: true
